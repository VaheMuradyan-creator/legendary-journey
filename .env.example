# Copy to .env.local for local dev. You MUST add the SAME two variables in Vercel manually.
# (They are NOT created automatically — only BLOB_READ_WRITE_TOKEN is auto-added when you enable Blob.)
ADMIN_PASSWORD=your-password-here
SESSION_SECRET=your-long-random-secret-here
